import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    inputContainer: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginBottom: "2%"
    },
    addVendorBtn: {
        padding: "1% !important",
    }
    
})

